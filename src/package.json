{
  "name": "mysuru-swachha-portal",
  "private": true,
  "version": "0.1.0",
  "description": "Mysuru Swachha Portal — MCC 65-ward operations, civic complaints, and 600 TPD logistics simulator",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:api": "npm run dev -w backend",
    "dev:web": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "seed": "npm run seed -w backend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
